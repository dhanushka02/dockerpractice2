PORT=3000
NODE_ENV=development

# Use your local PostgreSQL database now.
# Later, point this same URL at your PostgreSQL image/container.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/pos_tool

# Decimal rate. 0.10 means 10%.
TAX_RATE=0.10
